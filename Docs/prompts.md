### Claude Code

Based on the provided specifications, create a detailed step by step implementation plan and create a downloadable file 'implementationPlan.md'.   

This file should contain the overall plan to implement the project, starting by the backend and moving forward to the frontend.

It should contain the next details:


Technologies selected for the backend: Typescript with node and fastify. 

Technologies selected for the frontend: React with Typescript on Vite.


Link pointing to backendImplementationDetails.md (which you should as well create to simplify the backend creation), containing all required details of the backend implementation.


Link Pointing to frontendImplementationDetails.md (also created by you), containing all required details of the frontend implementaion.

Code should be: modular, it should contain unit test, and all .env variables should be inside a folder called config, not in root project.

Testing in the backend mus be done using by default fastify features, test in the the frontend should be created with Vitest (due to it's native support for typescript)

All details should address configurations.

The main goal is to have this documents as part of the Specifications to develop the app using any AI tool as assistant. Thus it must be clear, well structured, detailed and efficient.

If any Clarification questions are needed, start by asking them and then move on with the task.

### Github Copilot

Follow this implementation plan, let's start by building the backend first.

The backend was implemented successfully, now let's build the frontend, follow the same implementation plan but take the frontend branch.